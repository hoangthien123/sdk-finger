#pragma once

// SDKDDKVer.h ; The Windows platform with the highest level of availability is defined.

// Previous Windows - If you want to build your application for the platform, you must have SDKDDKVer.h before
// WinSDKVer.h And set the _WIN32_WINNT macro to the platform you want to support.

#include <SDKDDKVer.h>
